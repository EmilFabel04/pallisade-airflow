# Astro Runtime includes many provider packages. Add only what this project needs.
#
# dbt is used by dags/pipeline_dbt_bigquery.py to build the BigQuery analytics models.
# Pin loosely to avoid frequent breakage; adjust if your dbt project requires a specific version.
dbt-core>=1.7,<2.0
dbt-bigquery>=1.7,<2.0
