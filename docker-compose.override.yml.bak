# NOTE: This file was previously a backup of a local override.
# Keeping it in the repo is confusing and can break tooling expectations.
# It is intentionally left as a no-op placeholder; use docker-compose.yml (ports remapped)
# or start via Astronomer (Astro) with the instructions in README.
