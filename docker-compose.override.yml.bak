# Local-only overrides to avoid host port conflicts.
# This file is intentionally safe to keep in git: it uses non-default host ports.

services:
  airflow-webserver:
    ports:
      - "127.0.0.1:8081:8080"

  postgres:
    ports:
      - "127.0.0.1:5433:5432"
