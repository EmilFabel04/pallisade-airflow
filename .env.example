# Local dev only. Copy to .env

AIRFLOW_UID=50000
AIRFLOW_GID=0

# Set a Fernet key for local encryption (generate via `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`)
AIRFLOW__CORE__FERNET_KEY=

# Optional: set a fixed secret key
AIRFLOW__WEBSERVER__SECRET_KEY=
