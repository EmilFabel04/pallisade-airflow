# NOTE: This patch is additive guidance only. If you already have these env vars set, keep your existing values.
# Ensure the Airflow containers have a google_cloud_default connection available via env.
# Add under each airflow-* service that runs tasks (scheduler, webserver, triggerer, worker if present):
#
# environment:
#   AIRFLOW_CONN_GOOGLE_CLOUD_DEFAULT: "google-cloud-platform://?extra__google_cloud_platform__project=bigen-484520"
#   GOOGLE_APPLICATION_CREDENTIALS: "/opt/airflow/secrets/sa-key.json"
# volumes:
#   - ./secrets/sa-key.json:/opt/airflow/secrets/sa-key.json:ro
